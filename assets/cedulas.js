(()=>{'use strict';
const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let data,categories,fallback;
const teams=c=>{const live=Array.from(new Set([...Object.keys(c.rosters||{}),...(c.standings||[]).flatMap(t=>(t.rows||[]).map(r=>r[1]))])).filter(Boolean);return (live.length?live:(fallback[c.name]||[]).map(t=>t.name)).sort((a,b)=>a.localeCompare(b,'es'))};
const selected=()=>categories.find(c=>String(c.id)===$('cat').value);
function options(el,values){el.innerHTML=values.map(v=>'<option>'+esc(v)+'</option>').join('');el.disabled=false}
function changeCategory(){const list=teams(selected());options($('home'),list);options($('away'),list);if(list.length>1)$('away').selectedIndex=1}
function roster(c,team){const names=c.rosters?.[team]||[];const rows=names.length?names:Array(18).fill('');return '<h3>'+esc(team)+'</h3>'+(!names.length?'<p>La fuente no publica nombres para este equipo. Completa y verifica la plantilla antes de firmar.</p>':'')+'<div class="roster"><table><thead><tr><th>#</th><th>Jugador</th><th>Dorsal</th><th>Goles</th><th>TA</th><th>TR</th><th>Firma</th></tr></thead><tbody>'+rows.map((n,i)=>'<tr><td>'+(i+1)+'</td><td'+(!n?' contenteditable="true"':'')+'>'+esc(n)+'</td>'+Array(5).fill('<td contenteditable="true"></td>').join('')+'</tr>').join('')+'</tbody></table></div>'}
function sheet(c,home,away){return '<article class="sheet"><div class="sheet-head"><img src="./assets/liga-logo.webp" alt="Liga Juventino Rosas"><div><h2>Liga Juventino Rosas</h2><p>'+(away?'CÉDULA ARBITRAL':'PLANTILLA DE EQUIPO')+' · '+esc(c.name)+'</p></div></div><p class="pending">PENDIENTE DE VALIDACIÓN Y FIRMA DE LA LIGA</p><h2>'+esc(home)+(away?' vs '+esc(away):'')+'</h2><p class="meta"><span>Fecha: '+esc($('date').value||'Por confirmar')+'</span><span>Campo: '+esc($('field').value||'Por confirmar')+'</span><span>Árbitro: '+esc($('referee').value||'Por asignar')+'</span></p>'+roster(c,home)+(away?roster(c,away):'')+'<h3>Observaciones / resultado</h3><div class="notes" contenteditable="true"></div><div class="signatures"><span>Árbitro</span><span>Delegado / capitán</span><span>Validación de la liga</span></div><small>Generada en esta página con datos públicos capturados: '+esc(data.captured_at_utc||'fecha no disponible')+'. Los campos editables se conservan solo mientras esta página esté abierta.</small></article>'}
function generate(){const c=selected(),h=$('home').value,a=$('away').value;if(h===a){$('status').textContent='Selecciona dos equipos diferentes.';return}$('sheets').innerHTML=sheet(c,h,a);$('print').disabled=false;$('status').textContent='Cédula lista. Completa los campos y revisa antes de imprimir.'}
$('cat').onchange=changeCategory;$('generate').onclick=generate;$('print').onclick=()=>window.print();
$('all').onclick=()=>{$('sheets').innerHTML=categories.flatMap(c=>teams(c).map(t=>sheet(c,t))).join('');$('print').disabled=false;$('status').textContent='Plantillas generadas para todos los equipos de todas las categorías.'};
Promise.all(['./data/official-live.json','./data/cedulas-teams.json'].map(url=>fetch(url,{cache:'no-store'}).then(r=>{if(!r.ok)throw Error('HTTP '+r.status);return r.json()}))).then(([d,f])=>{fallback=f;data=d;categories=Object.values(d.categories||{});if(!categories.length)throw Error('Sin categorías');$('cat').innerHTML=categories.map(c=>'<option value="'+esc(c.id)+'">'+esc(c.name)+'</option>').join('');$('cat').disabled=false;changeCategory();$('generate').disabled=false;$('all').disabled=false;$('status').textContent='Datos cargados. Puedes generar una cédula o las plantillas de todos los equipos.';const id=new URLSearchParams(location.search).get('cedula');if(id){for(const c of categories){const match=(c.cedulas||[]).find(x=>String(x.id)===id);if(match){$('cat').value=String(c.id);changeCategory();$('home').value=match.local;$('away').value=match.away;if($('home').value&&$('away').value)generate();break}}}}).catch(()=>{$('status').textContent='No se pudieron cargar los datos. Revisa tu conexión y recarga la página; no se generaron plantillas vacías como si fueran verificadas.'});
})();
